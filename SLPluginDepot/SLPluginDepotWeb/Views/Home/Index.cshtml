@{
    ViewData["Title"] = "Home Page";
}

@* <style>
    /* Reset and basics */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html, body {
        height: 100%;
        background-color: #0b0c0e; /* Very dark */
        color: #c8c8c8; /* Soft grey */
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        overflow: hidden; /* prevent scrolling */
    }

    nav {
        position: fixed;
        top: 0;
        width: 100%;
        height: 60px;
        background: #1a1a1a;
        border-bottom: 1px solid #333;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 30px;
        z-index: 1000;
        color: #f0f0f0;
        font-weight: 600;
        font-size: 1.2rem;
        letter-spacing: 1.1px;
        user-select: none;
        box-shadow: 0 2px 8px rgba(0,0,0,0.6);
    }

        nav .btn-browse {
            background-color: #55c7ff;
            border: none;
            padding: 8px 18px;
            border-radius: 5px;
            color: #0b0c0e;
            font-weight: 700;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
            user-select: none;
        }

            nav .btn-browse:hover {
                background-color: #2fa1db;
            }

    .content-container {
        height: calc(100vh - 60px); /* fill viewport minus navbar */
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 0 20px;
        text-align: center;
        flex-direction: column;
    }

    .homepage-hero {
        max-width: 800px;
    }

        .homepage-hero h1 {
            font-size: 3.5rem;
            margin-bottom: 0.6rem;
            color: #55c7ff; /* icy blue */
            text-shadow: 0 0 10px #55c7ff88;
        }

        .homepage-hero p {
            font-size: 1.3rem;
            line-height: 1.6;
            color: #a0a0a0;
            margin-bottom: 2rem;
        }

        .homepage-hero a {
            color: #55c7ff;
            text-decoration: none;
            font-weight: 600;
            border-bottom: 2px solid transparent;
            transition: border-bottom 0.3s ease;
        }

            .homepage-hero a:hover {
                border-bottom: 2px solid #55c7ff;
            }

        /* Small subtle SCP vibe */
        .homepage-hero p::before {
            content: "⚠️ ";
            opacity: 0.6;
        }

    /* Browse Plugins button below description */
    .browse-plugins-btn {
        background-color: #55c7ff;
        color: #0b0c0e;
        padding: 15px 40px;
        font-size: 1.5rem;
        font-weight: 700;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        box-shadow: 0 0 15px #55c7ff66;
        transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease;
        user-select: none;
        text-decoration: none;
        display: inline-block;
    }

        .browse-plugins-btn:hover {
            background-color: #2fa1db;
            box-shadow: 0 0 20px #2fa1dbaa;
            color: #f0f0f0;
            transform: scale(1.1);
        }
</style>

<nav>
    <div>SCP Fanmade Plugin Browser</div>
</nav>

<div class="content-container">
    <div class="homepage-hero">
        <h1>Welcome to SCP Plugin Depot</h1>
        <p>
            A fanmade browser for SCP: Secret Laboratory plugins.<br />
            Discover, rate, and share your favorite mods!
        </p>
        <a href="/Plugin" class="browse-plugins-btn">Browse Plugins</a>
    </div>
</div>
 *@